<Window x:Class="ezytask.shell.Login.LoginFormView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="LoginFormView" Width="300" Height="130" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Style="{DynamicResource TheLabel}">Email:</TextBlock>
        <TextBox Grid.Row="0" Grid.Column="1" Name="txtEmail" Margin="5" Style="{DynamicResource TheTextBox}"></TextBox>
        
        <Button Grid.Row="1" 
                Grid.Column="2" 
                Margin="5" 
                VerticalAlignment="Top" 
                Name="btnLogin" 
                Click="btnLogin_Click"
                IsDefault="True">OK</Button>
    </Grid>
</Window>
